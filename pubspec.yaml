name: meds_tracker
description: "A Flutter project for keeping track of meds."
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '^3.1.1'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  shared_preferences: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
